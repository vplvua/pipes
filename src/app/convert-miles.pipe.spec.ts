import { ConvertMilesPipe } from './convert-miles.pipe';

describe('ConvertMilesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMilesPipe();
    expect(pipe).toBeTruthy();
  });
});
